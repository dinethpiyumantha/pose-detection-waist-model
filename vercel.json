{
    "version": 2,
    "builds": [
        {
            "src": "./main.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        { "src": "/api/predict/keypoints", "dest": "api/predict/keypoints" },
        { "src": "/(.*)", "dest": "/" }
      ]
}